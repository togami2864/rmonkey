{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug file.rs",
            "cargo": {
                "args": [
                    "run",
                    "build"
                ],
            },
            "cwd": "${workspaceFolder}"
        },

    ]
}